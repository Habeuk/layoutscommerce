# Schema for the configuration files of the layoutscommerce module.
layoutscommerce.settings:
  type: config_object
  label: 'layoutscommerce settings'
  mapping:
    example:
      type: string
      label: 'Example'
